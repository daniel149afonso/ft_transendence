/**
 * debug.ts — interrupteur global du mode debug physique.
 *
 * true  → hitboxes visibles (rectangles rouges), utile pendant le dev.
 * false → rendu propre, aucun overhead de dessin.
 */
export const DEBUG = false;
